interface:
  display_name: "Kamae Scala Review"
  short_description: "Review robust Scala domain code"
  default_prompt:
    - "Use $kamae-scala-review to review this Scala domain diff for validation, invariants, PII, and tests."
    - "Use $kamae-scala-review to audit this module for boundary defense and error handling."
    - "Use $kamae-scala-review to check this PR for state transition safety and persistence consistency."
